#logging:
#  pattern:
#    console: "%d -%msg%n"
#    level: debug
spring:
    datasource:
      url: jdbc:mysql://localhost:3306/sell?useUnicode=true&characterEncoding=UTF8&useSSL=false&serverTimezone=UTC
      driver-class-name: com.mysql.cj.jdbc.Driver
      username: root
      password: root
    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true






server:
  port: 9090
  servlet:
    context-path: /sell
#  file: E:/workspace/IdeaProjects/sell/var/log/tomcat/sell.log